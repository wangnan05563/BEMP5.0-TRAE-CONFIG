# 测试数据准备文档模板

> 根据测试用例集，通过 Oracle MCP / MySQL MCP 查询数据库生成标准化测试数据。

## 一、数据需求汇总

| 用例编号 | 用例名称 | 所需数据 | 数据来源表 |
|----------|----------|----------|-----------|
| TC-XXX-001 | [用例名] | [数据描述] | [表名] |

## 二、数据查询与准备

### 2.1 [模块名称] 测试数据

**查询现有数据**：

```sql
SELECT * FROM [表名] WHERE [条件];
```

**缺口识别**：

| 数据项 | 现有数量 | 需求数量 | 缺口 | 补充SQL |
|--------|---------|---------|------|---------|
| [数据项] | [数量] | [数量] | [数量] | [INSERT SQL] |

**补充数据**：

```sql
INSERT INTO [表名] ([字段列表]) VALUES ([值列表]);
```

**验证数据就绪**：

```sql
SELECT COUNT(*) FROM [表名] WHERE [条件];
```

## 三、数据使用说明

| 数据项 | 适用用例 | 使用方式 | 注意事项 |
|--------|---------|---------|---------|
| [数据项] | [用例列表] | [使用方式] | [注意事项] |

## 四、数据清理策略

```sql
-- 测试完成后清理（按创建时间标记）
DELETE FROM [表名] WHERE [条件] AND [时间标记];
```